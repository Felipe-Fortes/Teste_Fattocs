services:
  - type: web
    name: cadastro-tarefas
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: DATABASE_URL
        fromDatabase:
          name: tarefas-db
          property: connectionString
